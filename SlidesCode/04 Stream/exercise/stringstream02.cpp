#include <iostream>
#include <sstream>

int main(){
    std::ios::sync_with_stdio(false);
    double tao = 6.28;
    std::cout << tao;
    std::cout << std::endl;
    //std::cout << tao << std::endl;
    // std::string initial_quote = "I like eating apples";

    // std::stringstream ss(initial_quote);
    // std::string fs,sc,extracted_quote;
    // ss >> fs >> sc;
    // std::getline(ss,extracted_quote);
    // std::cout << fs << " " << sc << extracted_quote << std::endl;


    
    return 0;
}